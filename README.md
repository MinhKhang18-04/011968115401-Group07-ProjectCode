# Điều phối CPU (FCFS & SJF)

## Cấu trúc thư mục:
- `/src`: Chứa mã nguồn C++.
- `/docs`: Chứa báo cáo cuối cùng.
- `/assets`: Chứa hình ảnh kết quả và sơ đồ Gantt.

## Cách chạy:
1. Sử dụng trình biên dịch C++ (g++).
2. Lệnh: `g++ src/fcfs.cpp -o fcfs && ./fcfs`
